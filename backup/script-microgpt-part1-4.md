>>> BOS 的双重身份
@type: animation
@rect: safe
@enter: fade

[0s: 屏幕中央显示名字 "emma" 的 token 序列，
 从左到右排列成六个方块链：
 BOS(绿色标签"开始") → e → m → m → a → BOS(红色标签"结束")]
[3s: 上方出现两条标注箭头：
 左箭头指向开头 BOS——"告诉模型：要开始生成名字了"
 右箭头指向结尾 BOS——"告诉模型：名字到这里结束"]
[6s: 下方切换为推理流程动画：
 BOS → 模型预测 → j 弹出 → 继续预测 → o → h → n →
 模型预测 → BOS 弹出（红色闪烁）→ 停止 → 输出 "john"]

当microgpt生成"emma"这个名字的时候
BOS 是"序列边界符"
名字 "emma" 被编码成
BOS、4、12、12、0、BOS
开头的 BOS 告诉模型："要开始生成名字了"
结尾的 BOS 告诉模型："名字到这里结束"
