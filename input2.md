
>>> 为什么 200 行就够
@type: textcard
@rect: center-80
@enter: fade-up
@exit: fade
@align: left
@duration: 5s

> PyTorch 给你的 99%，是效率：
>
> GPU 加速 · 自动并行 · 内存管理 · 自动求导
>
> 这些让训练快了几百倍——但不是算法本身。

karpathy 把所有效率优化剥掉，
只留**算法骨架**，就是 **200 行**。

>>> 文件六块鸟瞰
@type: animation
@rect: safe
@enter: fade

[0s: 左侧出现竖长的文件图标，标注 "microgpt.py · 200 行"，
 内部从上到下分为六个色块区域]
[1s: 第一块（浅蓝色）亮起，右侧弹出标签 "① 数据 + 分词器（14-27 行）"]
[2s: 第二块（绿色）亮起，标签 "② 自动求导引擎（29-72 行）"]
[3s: 第三块（橙色）亮起，标签 "③ 参数初始化（74-90 行）"]
[4s: 第四块（紫色）亮起，标签 "④ 前向传播（92-144 行）"]
[5s: 第五块（红色）亮起，标签 "⑤ 训练循环（146-184 行）"]
[6s: 第六块（金色）亮起，标签 "⑥ 推理采样（186-200 行）"]
[8s: 六块全部点亮，底部加虚线分隔和注释：
 "上集 ①② ─ 中集 ③④ ─ 下集 ⑤⑥"]

六块：数据分词、自动求导、参数初始化、前向传播、训练循环、推理采样。

上集讲前两块，中集讲三四块，下集讲五六块。

>>> 分词器：字符和数字的翻译
@type: animation
@rect: safe
@enter: fade

[0s: 屏幕中央显示大字 "计算机只认数字，不认文字"]
[2s: 左侧出现文字 "emma"（每个字母用不同颜色），
 中间一个双向箭头图标标注 "Tokenizer 分词器"，
 右侧出现数字序列 [4, 12, 12, 0]（颜色对应左侧字母）]
[4s: 下方展开字母表对照表：a=0, b=1, c=2 ... e=4 ... m=12 ... z=25，
 "e""m""m""a" 各自连线到对应位置并高亮]
[7s: 字母表末尾出现一个特殊标记 "BOS = 26"，用 accent 色高亮脉冲]
[8s: 顶部出现总结框 "vocab_size = 27（26 个字母 + 1 个 BOS）"]

计算机只认数字，不认文字。

**分词器**就是字符和数字之间的双向翻译：
"emma" → [4, 12, 12, 0]，
[4, 12, 12, 0] → "emma"。

microgpt 的分词器极简：
26 个字母各有一个 id，a=0，b=1，……z=25，
再加一个特殊 token **BOS**，id=26。

词表大小：**vocab_size = 27**。

>>> BOS 的双重身份
@type: animation
@rect: safe
@enter: fade

[0s: 屏幕中央显示名字 "emma" 的 token 序列，
 从左到右排列成六个方块链：
 BOS(绿色标签"开始") → e → m → m → a → BOS(红色标签"结束")]
[3s: 上方出现两条标注箭头：
 左箭头指向开头 BOS——"告诉模型：要开始生成名字了"
 右箭头指向结尾 BOS——"告诉模型：名字到这里结束"]
[6s: 下方切换为推理流程动画：
 BOS → 模型预测 → j 弹出 → 继续预测 → o → h → n →
 模型预测 → BOS 弹出（红色闪烁）→ 停止 → 输出 "john"]

BOS 是"序列边界符"。

名字 "emma" 被编码成：
**BOS、4、12、12、0、BOS**

**开头的 BOS** 告诉模型："要开始生成名字了。"
**结尾的 BOS** 告诉模型："名字到这里结束。"

一个 token id 同时承担开始和结束——极简设计，省一个 id。

推理时，模型从 BOS 出发，预测到 BOS 时停止。

>>> 分词器代码
@type: code
@rect: center-80
@enter: fade
@source: microgpt.py
@range: 14-27
@reveal: line-by-line
@highlight: 24,25,26

先下载名字数据集，每行一个英文名字，总共三万多个。
打乱顺序，避免模型按字母顺序记忆。

字符去重排序，得到 26 个字母的列表，这就是词表。
每个字母的下标就是它的 token id。

BOS = 26，vocab_size = 27。

编码时用 uchars.index 把每个字符查出下标，简单直接。
整个分词器，不到 10 行。

（停顿）

>>> 自动求导：为什么需要它
@type: animation
@rect: safe
@enter: fade

[0s: 中央显示公式框 "loss = f(参数₁, 参数₂, ..., 参数₄₁₉₂)"，
 强调 loss 依赖几千个参数]
[3s: 画面分成左右两栏：
 左栏标题 "简单情况 ✓"：显示 "loss = (w-5)²"，
 下方用绿色手推出 "∂loss/∂w = 2(w-5)"
 右栏标题 "GPT 的 loss ✗"：显示一堆嵌套函数
 "softmax(linear(relu(linear(embed(x))))...)"，用红色大叉]
[6s: 两栏消失，中央出现高亮大字 "自动求导 = 自动算出所有偏导"]
[8s: 下方出现链式法则公式：
 "dz/dx = dz/dy × dy/dx"，
 标注 "原理只有一条：链式法则"，公式高亮脉冲]

训练需要梯度——每个参数相对 loss 的偏导数。

问题是，GPT 的 loss 是几千个参数经过十几层运算叠出来的，
不可能每次改模型结构都手推偏导。

**自动求导**自动算出所有梯度，
不管 loss 是怎么算出来的。

原理只有一条：**链式法则**——
如果 z 依赖 y，y 依赖 x，
那么 dz/dx = dz/dy × dy/dx。

把这条规则递归地应用到整张**计算图**，
就能从 loss 一路反推到每个参数。

>>> 计算图的直觉
@type: animation
@rect: safe
@enter: fade

[0s: 顶部依次淡入四行代码：
 "a=2, b=3" → "c = a×b = 6" → "d = c+1 = 7" → "loss = d² = 49"]
[2s: 下方开始构建计算图——节点从上到下依次出现：
 顶部两个圆形节点 a(2) 和 b(3)，
 中间乘号节点连出 c(6)，
 再下方加号节点连出 d(7)，
 底部平方节点连出 loss(49)]
[5s: 每条边旁标注局部偏导数值（依次淡入）：
 a→c 边: "∂c/∂a = b = 3"
 b→c 边: "∂c/∂b = a = 2"
 c→d 边: "∂d/∂c = 1"
 d→loss 边: "∂loss/∂d = 2d = 14"]
[8s: 底部出现问题框 "d(loss)/d(a) = ?"，
 从 loss 节点到 a 节点画出高亮反向路径，
 路径旁依次标出 14 × 1 × 3 = 42，答案 "42" 放大弹出]

看一个具体例子：
a=2，b=3，c=a×b=6，d=c+1=7，loss=d²=49。

每一步运算生成一个节点，节点之间的依赖关系构成一张图——
这就是**计算图**。

图在前向计算时自动建好。

问：d(loss)/d(a) 等于多少？
我们稍后用反向传播算，答案是 **42**。

>>> Value 类：四个字段
@type: code
@rect: center-80
@enter: zoom-in
@source: microgpt.py
@range: 30-37
@reveal: line-by-line
@highlight: 33,34,35,36

Value 类 = 计算图里的一个节点，只记四样东西：

**data**：节点的数值，前向计算时填入。
**grad**：对 loss 的梯度，反向传播时填入。
**_children**：这个节点从哪些节点算出来的。
**_local_grads**：对每个子节点的局部偏导，链式法则用。

前两个前向填，后两个反向用。

>>> 运算符重载：自动记录链式法则
@type: animation
@rect: safe
@enter: fade

[0s: 画面分为左右两栏：
 左栏标题 "加法 c = a + b"，下方显示 Python 代码片段 "__add__"，
 底部画出节点图：a → (+) ← b → c，两条边标注 "1" 和 "1"
 右栏标题 "乘法 c = a × b"，下方显示代码片段 "__mul__"，
 底部画出节点图：a → (×) ← b → c，两条边标注 "b" 和 "a"]
[3s: 左栏底部淡入数学公式 "∂c/∂a = 1, ∂c/∂b = 1"
 右栏底部淡入 "∂c/∂a = b.data, ∂c/∂b = a.data"]
[6s: 两栏缩小上移，底部出现一行四个小卡片（依次弹入）：
 "x^n → n·x^(n-1)" | "log(x) → 1/x" | "exp(x) → exp(x)" | "relu(x) → 0或1"]
[8s: 所有内容上方出现总结高亮文字：
 "每次 a+b、a×b，Python 自动把偏导规则记进节点"]

加法：c = a + b。
局部偏导：对 a 是 1，对 b 是 1。

乘法：c = a × b。
局部偏导：对 a 是 b，对 b 是 a——高中微积分。

Python 的运算符重载帮我们在每次 a+b、a×b 时，
**自动把这条规则记进节点**。

幂、对数、指数、ReLU，每个都只有一行。
减法和除法用已有算子组合，不需要单独写梯度。

>>> backward：13 行反向传播
@type: code
@rect: center-80
@enter: zoom-in
@source: microgpt.py
@range: 59-72
@reveal: line-by-line
@highlight: 69,70,72

反向传播只有 **13 行**。

**第一步**：从 loss 出发做拓扑排序——
梯度必须从后往前算，每个节点等下游算完才能轮到自己。

**第二步**：loss 自身的梯度设为 1。

**第三步**：按反向顺序遍历，
用局部偏导乘以下游梯度，用 **+=** 累加到每个节点。

注意是 +=，不是 =——
一个节点可能被多条路径共用，必须累加所有下游的贡献。

>>> 手算验证反向传播
@type: animation
@rect: safe
@enter: fade

[0s: 重新出现刚才的计算图：a(2) → c(6) → d(7) → loss(49)，b(3) → c，
 所有节点旁显示灰色 "grad=?"]
[2s: loss 节点亮起金色，标注 "grad = 1"（起点），
 旁边注释 "∂loss/∂loss = 1"]
[4s: 黄色高亮线从 loss 传到 d，d 节点亮起：
 显示计算 "loss=d², ∂loss/∂d = 2×7 = 14"，标注 "grad = 14"]
[6s: 高亮线从 d 传到 c，c 节点亮起：
 显示 "d=c+1, ∂d/∂c = 1, 14×1 = 14"，标注 "grad = 14"]
[8s: 高亮线从 c 传到 a，a 节点亮起：
 显示 "c=a×b, ∂c/∂a = b = 3, 14×3 = 42"，
 标注 "grad = 42" 放大高亮弹出]
[10s: 底部出现总结框：
 "autograd 引擎不到 50 行，实现了 PyTorch 自动求导的核心逻辑"]

手算那个例子来验证。

loss = d²，d=7，∂loss/∂d = 2×7 = **14**。

d = c+1，加法偏导 1，∂loss/∂c = 14×1 = **14**。

c = a×b，乘法偏导对 a 是 b=3，∂loss/∂a = 14×3 = **42**。

答案是 42，和之前一致。

（停顿）

这套 autograd 引擎不到 **50 行**，
实现了 PyTorch 自动求导的核心逻辑。

>>> 上集小结
@type: textcard
@rect: center-80
@enter: fade-up
@exit: fade
@align: left
@duration: 5s

> 上集回顾：
>
> · GPT = 下一个词预测器（参数 + loss + 梯度 + 优化器）
> · 分词器 = 字符 ↔ 数字（vocab_size=27，BOS 双重角色）
> · Autograd = 计算图 + 链式法则，50 行自动算梯度

中集解剖 Transformer 内部：
注意力是什么，MLP 是什么，它们各自解决什么问题。

>>> 片尾
@type: textcard
@rect: center-60
@enter: fade
@exit: none
@style: hero
@align: center
@duration: 4s

> microgpt.py · 上集
> by @karpathy
>
> 感谢收看，中集见
