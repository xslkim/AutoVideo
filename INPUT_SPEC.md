# 输入文档写作规范

> 本文档定义口播稿的格式要求。
> 口播稿是唯一的输入文件，旁白文字和素材描述写在同一个文件中。

---

## 一、整体结构

```markdown
# 视频标题

开场旁白文字（可选，显示标题卡）

>>> 第一个素材标题
**[类型]**
素材描述...

这个素材展示期间的旁白文字。

>>> 第二个素材标题
**[类型]**
素材描述...

这个素材展示期间的旁白文字。

（长停顿）

>>> 下一个素材标题
**[类型]**
素材描述...

旁白文字...
```

**规则：**
- 第一行必须是 `# 标题`，自动提取为视频标题和片尾信息
- `>>>` 标记开始一个新的素材块，同时承担三个职责：
  1. 定义素材切换时间点
  2. 引出素材描述（紧跟在 `>>>` 行之后）
  3. 划分 TTS 并行单元
- 第一个 `>>>` 之前的文字为开场旁白（使用标题卡作为画面）
- 每个 `>>>` 后面到下一个 `>>>` 之间的旁白文字，在该素材展示期间播放

---

## 二、`>>>` 素材块语法

```markdown
>>> 素材标题
**[类型]**
描述内容第一行
描述内容第二行

旁白文字从这里开始（空行之后）。
```

**解析规则：**
1. `>>>` 行：素材标题（`>>>` 后的文字）
2. 下一行：`**[类型]**` 标记（必须）
3. 后续非空行：素材描述（直到遇到空行）
4. 空行之后：旁白文字（直到下一个 `>>>` 或文件结束）

**注意：** 素材描述内部不要有空行。如需多段描述，用连续的行书写。

---

## 三、素材类型

| 类型标记 | 含义 | Agent 生成方式 |
|---------|------|---------------|
| `[动画]` | 有时间轴的动态画面 | React 组件 + Remotion 动画 API |
| `[截图]` | 模拟静态截图 | React 组件 + 缓慢缩放(Ken Burns) |
| `[录屏]` | 模拟软件操作过程 | React 组件模拟 UI + 逐步动画 |
| `[文字卡]` | 纯文字画面 | 通用 TextCard 组件 |
| `[代码]` | 代码展示/打字效果 | 代码高亮组件 + 逐行出现 |

---

## 四、字幕控制

旁白文字会直接转为 TTS 语音和屏幕字幕。

### 字幕行长度上限

| 画面比例 | 屏幕宽度 | 字幕字号 | 每行上限 | 说明 |
|---------|---------|---------|---------|------|
| 16:9 横屏 | 1920px | 36px | **20 个中文字符** | B站/YouTube 标准 |
| 9:16 竖屏 | 1080px | 32px | **14 个中文字符** | 抖音/小红书 |
| 1:1 方屏 | 1080px | 34px | **16 个中文字符** | Instagram |

> 英文和数字按 0.5 个中文字符计。标点符号按 1 个字符计。

### 用空行分割字幕块

每个空行分隔一个**字幕块**。每个字幕块在屏幕上独立显示，对应一次字幕刷新。

```markdown
每个月看到云服务器的账单，我都肉疼。

我有几台 Windows 服务器跑在云上，
按流量计费。
```

上面有 2 个字幕块：
- 块 1："每个月看到云服务器的账单，我都肉疼。"（一行显示）
- 块 2："我有几台 Windows 服务器跑在云上，按流量计费。"（TTS 连读，字幕两行显示）

### 用换行控制字幕行数

字幕块内的**硬换行**（行末直接回车）会被保留为字幕内的行分割：

```markdown
我有几台 Windows 服务器跑在云上，
按流量计费。
```

渲染效果：
```
我有几台 Windows 服务器跑在云上，
按流量计费。
```

**每个字幕块最多 2 行。** 超过 2 行的块自动截断第 3 行及以后到下一个字幕块。

### 字幕块 vs TTS 朗读

- 空行：字幕切换 + TTS 短暂停顿（~0.3 秒）
- 硬换行：仅影响字幕显示分行，TTS 连读不停顿
- 句号/问号：TTS 自然停顿，但字幕不切换

---

## 五、语速与停顿

```markdown
每个月看到云服务器的账单，我都肉疼。

（停顿）

我有几台 Windows 服务器跑在云上，
按流量计费。
```

| 标记 | 效果 |
|------|------|
| 空行 | TTS 短暂停顿 ~0.3s + 字幕切换 |
| `（停顿）` 或 `（pause）` | 额外 1 秒静音（不显示字幕） |
| `（长停顿）` | 额外 2 秒静音（可用于主题切换，替代原来的段落间隔） |
| 空行 × 2 | 等价于空行 + `（停顿）` |

**场景切换建议**：在两个主题之间用 `（长停顿）` 标记，视频会插入 2 秒黑屏过渡，类似原来段落之间的间隔效果。

---

## 六、重音与高亮

```markdown
限速要用 **WinDivert**——一个开源的网络数据包拦截库。
```

- `**加粗**` 的词在字幕中用强调色（`textAccent`）高亮显示
- 不影响 TTS 朗读

---

## 七、素材描述怎么写

**描述越具体，AI 生成的组件越准确。** 好的描述包含：

### 必写项

1. **画面内容**：画面上有什么元素
2. **数据/文案**：具体显示的文字、数字、名称

### 选写项（写了效果更好）

3. **动画流程**：元素出现的先后顺序、运动方式
4. **时间轴**：关键帧的时间点
5. **样式要求**：颜色、字号、布局等特殊要求
6. **代码块**：需要原样显示的内容用 ` ``` ` 包裹

### 描述质量对比

**差（太模糊）：**
```markdown
>>> 工具价格
**[动画]**
展示三个工具的价格。
```

**好（具体可执行）：**
```markdown
>>> 工具定价对比
**[截图]**
三列等宽卡片横向排列，每个卡片包含：
- NetLimiter：$29.95 / 台
- GlassWire：$49 / 年
- SoftPerfect Bandwidth Manager：$49 / 台
每个价格用红色大字标注，卡片用红色边框。
卡片从左到右依次淡入，间隔 0.5 秒。
```

---

## 八、文字卡的特殊写法

文字卡的**精确文案**必须用引号或引用块标出：

```markdown
>>> 黑盒金句
**[文字卡]**
画面中央显示：
> 「花钱买一个黑盒来省钱，这逻辑说不通。」
深色背景，文字金色，居中，淡入停留 2 秒后淡出。
```

---

## 九、代码展示素材

如果素材涉及展示代码，指定代码来源：

```markdown
>>> 令牌桶代码快进
**[代码]**
使用代码文件：TokenBucket.h
代码行逐行从上到下出现，每帧 2-3 行。
深色编辑器风格（VS Code Dark+），
行末有闪烁光标。3-5 倍速播放效果。
```

- `使用代码文件：xxx` 指定 `src/data/source-samples/` 中的文件名
- 如果不指定文件，也可以直接在 ` ``` ` 代码块里写内容

---

## 十、动画时间轴写法

对于复杂动画，建议写明时间轴：

```markdown
>>> 令牌桶原理图
**[动画]**
- 0-2s：桶满，3 个数据包到达并消耗水位，绿色放行
- 2-4s：水位降到 0，新数据包被阻断（红色 ×）
- 4-6s：水龙头继续滴水，水位缓慢上升
- 6-8s：水位够了，被阻断的包重新放行
```

Agent 会根据这些时间点生成对应的 `interpolate()` 关键帧。

---

## 十一、片尾

建议最后一个素材块为片尾信息：

```markdown
>>> 片尾信息
**[文字卡]**
黑底白字：
> MyProject
> C++ / Python / React
> 感谢收看
4 秒。
```

---

## 十二、完整示例

```markdown
# 我用 AI 两小时造了一个限速工具

每个月看到云服务器的账单，我都肉疼。

>>> 云账单截图
**[截图]**
模拟云服务商控制台费用页面：
- 深色面板，标题「流量费用明细 — 2025年3月」
- 表格：出站流量 580GB / ¥0.80/GB / 合计 ¥464
- 「合计: ¥464」用红色大字高亮
- 缓慢放大效果

我有几台 Windows 服务器跑在云上，
按流量计费。
每 GB 出站流量几毛钱，听起来不多，
但架不住有些程序特别能吃。

>>> 任务管理器动画
**[动画]**
仿 Windows 11 任务管理器网络标签页：
- 列：进程名 | PID | 网络使用
- 3 行数据动态更新：
  - Windows Update：20 MB/s
  - LogSync.exe：5 MB/s
  - crawler.py：8 MB/s
- 每行逐个黄色高亮，底部滑入：「月底流量费: ¥580」

Windows Update 后台狂拉补丁、
日志同步服务一股脑往外推数据、
某个写得不太好的爬虫程序跑起来没有节制。
月底一看账单，光流量费就好几百。

（长停顿）

>>> 工具定价对比
**[截图]**
三个等宽卡片横向排列：
- NetLimiter：$29.95 / 台
- GlassWire：$49 / 年
- SoftPerfect：$49 / 台
价格红色大字，红色边框，卡片依次淡入。

这需求不复杂，市面上有现成工具。
NetLimiter、GlassWire、SoftPerfect……
我挨个看了一遍，然后我看到了价格。

>>> 黑盒金句
**[文字卡]**
> 「花钱买一个黑盒来省钱，这逻辑说不通。」
金色文字，深色背景，居中，停留 2.5 秒。

花钱买一个黑盒来省钱，
这逻辑说不通。

（停顿）

我决定自己造一个。

>>> 片尾信息
**[文字卡]**
> MyProject
> C++ / Python / React
> 感谢收看
黑底白字，4 秒。
```

---

## 十三、自查清单

写完口播稿后，按以下清单检查：

- [ ] 第一行是 `# 标题`
- [ ] 每个素材块都有 `>>>` 标题行 + `**[类型]**` 标记
- [ ] 素材描述内部没有空行（空行会被解析为旁白开始）
- [ ] 每个字幕块（空行之间的旁白文字）不超过 2 行
- [ ] 每行字幕长度不超过目标比例的上限（16:9 → 20字，9:16 → 14字）
- [ ] 文字卡的精确文案用 `>` 引用块标出
- [ ] 代码展示素材指定了代码文件名或提供了代码块
- [ ] 复杂动画有时间轴描述
- [ ] 主题切换处有 `（长停顿）` 标记
- [ ] 最后一个素材块是片尾文字卡
- [ ] 总字数在预期范围（中文 ~200字/分钟，估算视频时长）
